name: quality
on: push

jobs:
  quality:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2
      - uses: erlef/setup-elixir@v1
        with:
          otp-version: "27.2"
          elixir-version: "1.18.1"
      - run: mix deps.get
      - run: mix quality

